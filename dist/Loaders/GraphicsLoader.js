class GraphicsLoader {
    constructor() {
    }
    loadGraphics(imageSource) {
        let image = new Image();
        image.src = imageSource;
        return image;
    }
}
//# sourceMappingURL=GraphicsLoader.js.map